# 飞书多维表格同步说明

这个版本已经支持把新的报名记录自动写入飞书多维表格。网站本身仍然可以正常报名、后台查看和下载；飞书同步只是额外备份。

## 1. 新建多维表格

在飞书里新建一个多维表格，建议表名叫：

`MSC海克斯大乱斗报名`

请在表格里建立这些字段，字段类型都先用“文本”即可：

- 报名编号
- 提交时间
- 报名类型
- 队伍名
- 联系人
- 联系方式
- 常用位置
- 接受拼队
- 队员信息
- 替补信息
- 备注

字段名要尽量和上面完全一致，否则飞书可能会拒绝写入。

## 2. 创建飞书自建应用

进入飞书开放平台，创建一个“企业自建应用”。

需要记录两个值：

- App ID
- App Secret

这两个值不要发到聊天里，后面直接填到 Render 环境变量。

## 3. 给应用开通多维表格权限

在飞书开放平台的应用后台里，给应用添加多维表格相关权限，至少需要能向多维表格新增记录。

飞书官方接口文档：

- 获取 tenant_access_token：https://open.feishu.cn/document/server-docs/authentication-management/access-token/tenant_access_token_internal
- 新增多维表格记录：https://open.feishu.cn/document/server-docs/docs/bitable-v1/app-table-record/create

添加权限后，如果页面提示需要发布版本或启用应用，请按飞书提示发布/启用。

## 4. 找到 app_token 和 table_id

打开你的飞书多维表格，浏览器地址里通常能看到两个关键值：

- `app_token`：多维表格应用 token，通常以 `base` 或类似字符串开头。
- `table_id`：当前数据表 ID，通常在 URL 的 `table=` 后面，或在打开具体数据表时的地址中。

如果你不确定哪个是哪个，可以把“去掉隐私内容后的链接截图”发来，我帮你判断。不要把 App Secret 发出来。

## 5. 在 Render 填环境变量

进入 Render 的服务页面：

`msc-hextech-signup` → `Environment`

添加这四个环境变量：

- `FEISHU_APP_ID`
- `FEISHU_APP_SECRET`
- `FEISHU_BITABLE_APP_TOKEN`
- `FEISHU_TABLE_ID`

原来的后台密码环境变量继续保留：

- `ADMIN_PASSWORD=mm123456`

保存后，Render 会重新部署；如果没有自动部署，就点右上角 `Manual Deploy`。

## 6. 测试

部署成功后，在报名页面提交一条测试报名。然后打开飞书多维表格，应该会新增一行。

如果网站报名成功但飞书没有新增，请优先检查：

- 字段名是否完全一致
- 应用是否已经添加多维表格写入权限
- 应用是否已经发布/启用
- App ID、App Secret、app_token、table_id 是否填错


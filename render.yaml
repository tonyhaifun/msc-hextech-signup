services:
  - type: web
    name: msc-hextech-signup
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /tmp/msc-signup-data
      - key: ADMIN_PASSWORD
        sync: false
